# HARNESS_MANIFESTO.MD — moved

**The manifesto now opens `RULINGS.md` at the repo root.** Read it there:
[`RULINGS.md`](RULINGS.md), Part I.

It moved on 2026-09-01 because the standard and the exceptions to it are
one document. `RULINGS.md` reads: the manifesto (the standard every
addition to the harness is assessed against), the bridge (how a session in
this repo is governed), then the rulings (the exceptions, each naming the
principle it bends).

This file is a pointer and holds no content of its own. A second copy of a
standard is a second standard, and the two drift — D-0010, and Article 7's
reason for not writing a record that changes no future action.
